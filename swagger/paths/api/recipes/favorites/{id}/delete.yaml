      tags:
        - Favorites
      summary: Remove a recipe from favorites
      operationId: removeFavorite
      description: Remove a recipe from the user's favorites list
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: recipeId
          required: true
          schema:
            type: string
          description: ID of the recipe to remove
          example: "64f1c2b1e8a3a12f5a1b2c3d"
      responses:
        '204':
        description: No Content
        '400':
        $ref: '../../../components/responses/400.yaml'
        '401':
        $ref: '../../../components/responses/401.yaml'
        '404':
    description: 'User or Recipes not found'
    content:
      application/json:
        schema:
          type: object
          required:
            - status
            - message
            - data
          properties:
            status:
              type: integer
              example: 404
            message:
              type: string
              example: NotFoundError
            data:
              type: object
              required:
                - message
              properties:
                message:
                  type: string
                  example: 'Contact not found'
  '500':
    $ref: '../../../components/responses/500.yaml'
